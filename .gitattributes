# Keep shell scripts and shared-core files LF on every platform so the fleet-core
# hash gate stays stable (a Windows checkout must not CRLF-ify a hashed file).
*.sh                   text eol=lf
core/**                text eol=lf
