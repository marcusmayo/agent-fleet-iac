# secrets & local artifacts — never commit
*.env
.env
*.pem
id_ed25519*
agentfleet*
.provision-flags
*.tfstate*

# personal per-agent contracts (may contain your /32) - never committed
agents/*.local.jsonc
